<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Shortcuts
    |--------------------------------------------------------------------------
    |
    | Shortcuts make it easier to access your bundle and save you some typing.
    | Register shortcuts in this array as key/value pairs where the key is the
    | shortcut name and the value is the period seperated path to the bundle.
    | Shortcut names need to be unique.
    */
    'shortcuts' => [
        // 'shortcut_name' => 'path.to.bundle'
    ],

    /*
    |--------------------------------------------------------------------------
    | Transform Lang Keys
    |--------------------------------------------------------------------------
    |
    | Transform lang value keys.
    | Available options: 'camel_case', 'none', 'snake_case', 'studly_case'.
    */
    'key_transform' => 'none',

    /*
    |--------------------------------------------------------------------------
    | Global Key Namespace
    |--------------------------------------------------------------------------
    |
    | If you keep your translations in a single file, it may be useful to use a
    | global key namespace. For example, if your translations are in a file
    | called 'translations', for every key you have to type 'translations.key'.
    | Set the namespace to 'translations' so you only have to type 'key'.
    */
    'global_key_namespace' => '',
];
